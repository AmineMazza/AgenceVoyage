{% extends 'base.html.twig' %}

{% block title %}Hotel{% endblock %}

{% block body %}
    <h1>Hotel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hotel.id }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ hotel.lieu }}</td>
            </tr>
            <tr>
                <th>Etoile</th>
                <td>{{ hotel.etoile }}</td>
            </tr>
            <tr>
                <th>Distance</th>
                <td>{{ hotel.distance }}</td>
            </tr>
            <tr>
                <th>Nombre_nuits</th>
                <td>{{ hotel.nombreNuits }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hotel_index', {'idH': idH}) }}">retour à la liste</a>

    <a href="{{ path('app_hotel_edit', {'id': hotel.id, 'idH': idH}) }}">Modifier</a>

    {{ include('hotel/_delete_form.html.twig') }}
{% endblock %}
