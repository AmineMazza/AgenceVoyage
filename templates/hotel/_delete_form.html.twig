<form method="post" action="{{ path('app_hotel_delete', {'id': hotel.id, 'idH': idH}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel.id) }}">
    <button class="btn">Supprimer</button>
</form>
