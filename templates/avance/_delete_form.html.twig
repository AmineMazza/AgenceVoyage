<form method="post" action="{{ path('app_avance_delete', {'id': avance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avance.id) }}">
    <button class="btn">Delete</button>
</form>
