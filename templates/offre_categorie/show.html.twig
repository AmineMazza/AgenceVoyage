{% extends 'base.html.twig' %}

{% block title %}OffreCategorie{% endblock %}

{% block body %}
    <h1>OffreCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ offre_categorie.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre_categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_categorie_index') }}">back to list</a>

    <a href="{{ path('app_offre_categorie_edit', {'id': offre_categorie.id}) }}">edit</a>

    {{ include('offre_categorie/_delete_form.html.twig') }}
{% endblock %}
