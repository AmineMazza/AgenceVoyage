{% extends 'base.html.twig' %}

{% block title %}Offre Categorie{% endblock %}

{% block body %}
    <h1>Offre Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ offre_categorie.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre_categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_categorie_index') }}">retour à la liste</a>

    <a href="{{ path('app_offre_categorie_edit', {'id': offre_categorie.id}) }}">Modifier</a>

    {{ include('offre_categorie/_delete_form.html.twig') }}
{% endblock %}
