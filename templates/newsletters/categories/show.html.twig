{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}

    <table class="table mt-10">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
    <a class="ml-5 text-blue-500" href="{{ path('app_newsletters_categories_index') }}">retour à la liste</a>


    {# <a href="{{ path('app_newsletters_categories_edit', {'id': category.id}) }}">edit</a> #}

    {# {{ include('newsletters/categories/_delete_form.html.twig') }} #}
{% endblock %}

